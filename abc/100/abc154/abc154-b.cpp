#include <algorithm>
#include <cstdio>
#include <cstring>
#include <queue>
#include <utility>
#include <vector>
using namespace std;
typedef long long ll;

int main() {
  int n;
  char s[110];
  scanf("%s", s);
  n = strlen(s);
  for (int i = 0; i < n; i++) {
    printf("x");
  }
  printf("\n");
}